server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/medibook?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Password@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: 4c36e74ef39604
    password: cbe461d41a4a52
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  jwt:
    secret: MediBookSecretKey2026ForJWTTokenSigningPurposeMustBe256BitsLongAtLeast
    expiration-ms: 86400000

twilio:
  account-sid: "YOUR_TWILIO_ACCOUNT_SID"
  auth-token: "YOUR_TWILIO_AUTH_TOKEN"
  messaging-service-sid: "YOUR_MESSAGING_SERVICE_SID"

# ── Payment Configuration ──────────────────────────────────────
# You can set gateway to 'mock', 'razorpay', or 'stripe'
payment:
  gateway: razorpay
  cancellation-window-hours: 24
  razorpay:
    # Add your Razorpay API keys here (from Razorpay Dashboard)
    key-id: "rzp_test_SlfNRGdl1bjXg9"
    key-secret: "Ak0vKIqhqZ9Q1yit31o1Msms"
  stripe:
    # Add your Stripe secret key here (from Stripe Dashboard -> Developers -> API keys)
    # Starts with sk_test_ or sk_live_
    secret-key: ""
